services:
  app:
    build: .
    container_name: api-modelo
    mem_limit: 1024m
    network_mode: bridge
    volumes:
      - ./:/usr/app
    ports:
      - "9999:9999"
    logging:
      driver: "fluentd"
      options:
        fluentd-address: ipfluentd:portafluentd
        tag: api-modelo.logs
